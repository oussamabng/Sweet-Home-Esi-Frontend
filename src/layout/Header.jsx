import Logo from "../components/Logo";

const Header = () => {
  return (
    <div className="sweet-header bg-slate-800 py-2 flex justify-center">
      <Logo />
    </div>
  );
};

export default Header;